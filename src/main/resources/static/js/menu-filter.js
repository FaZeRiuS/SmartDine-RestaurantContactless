/**
 * Menu category filtering now uses HTMX on index.html and menu.html
 * (see /htmx/menu/categories-body). This file is kept as an empty stub so
 * old script tags do not 404 if any page still references it.
 */
