## Test-only server shutdown tuning.
## E2E tests use long-lived SSE connections; graceful shutdown waits for them and makes Surefire
## think the forked JVM is stuck. Immediate shutdown keeps the test suite fast and clean.
server.shutdown=immediate
spring.lifecycle.timeout-per-shutdown-phase=1s

spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.flyway.enabled=false
vapid.public.key=${VAPID_PUBLIC_KEY:BCfXj89XaWYPUsWu07F6GkYoQ4FcVKq_xq_aVm2Zg0FiSNNTmLyRrRPpJHHrb7Ad8ulKj8QGZCj5SAFSrjDgSs0}
vapid.private.key=${VAPID_PRIVATE_KEY:}
vapid.subject=${VAPID_SUBJECT:mailto:admin@smartdine.com}
frontend.menu.url=http://localhost:8081/menu?id=

# Performance/Security Overrides for E2E Tests (Fixes localhost timeouts)
server.compression.enabled=false
server.servlet.session.cookie.secure=false
app.upload.dir=target/uploads
# Match production default so E2E does not force-close the emitter every 30s (reconnect storm in logs).
app.sse.timeout-ms=3600000

# Avoid cross-test cache pollution
spring.cache.type=none
